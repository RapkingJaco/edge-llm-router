# 交接 — 回家繼續開發（edge-llm-router）

> 給「在家的新對話」接手用。設計已定案、規格已寫好、repo 骨架已建並推上 GitHub，**下一步是寫實作計畫 + 開始寫程式**。
> 更新：2026-07-24（下班前）。作者 Jacob（GitHub: RapkingJaco）。

---

## 0. 30 秒抓重點

- 專案：**`edge-llm-router`** — AI Agent 智慧 LLM 推論分流器的**展示平台**（不是聊天機器人）。
- **完整設計看這份（已在 repo 裡）**：[`docs/superpowers/specs/2026-07-24-edge-llm-router-design.md`](docs/superpowers/specs/2026-07-24-edge-llm-router-design.md) ← **新對話請先讀它**。
- 現況：規格 + README + .gitignore 已 commit 並 push。**還沒有任何程式碼。**
- 下一步：走 **superpowers:writing-plans** 寫實作計畫 → 照建置順序開工（先 `backends` + `sim`）。

---

## 1. 這專案在做什麼

很多使用者同時發 LLM 請求，**RL agent(PPO) 即時決定每個請求在 本機/邊緣/雲 哪裡跑**，最佳化 **TTFT + 成本**；使用者用**中文下方針**（「成本太高，多用本機」），**真的 LLM(Gemini)** 把白話轉成 RL 的獎勵權重，排程當場變。三面板網頁：左＝即時分流儀表板、中＝AI vs 基準線、右＝中文方針框。

---

## 2. 已鎖定的設計決策（細節在 spec，這裡是索引）

- **節點對應**：本機(弱手機)=模擬(錨定 benchmark)；**邊緣=你的 4070 真跑 Ollama**；雲端=真 Gemini（小額預付）。成本一律「自己標價」（雲端假裝很貴）。可插拔後端 `NodeBackend`/`InferResult` + 自動 fallback。
- **訓練/展示分離**：訓練全用（校準過的）模擬；demo 抽驗少量真打 Ollama/Gemini 標「✅實測」。＝Sim-to-Real。
- **Action**：`Discrete(3)` 逐請求 + ratio 曲線展示 + MLP。
- **核心 DL：權重條件化**（reward 權重當 obs 輸入、訓練隨機抽 w）→ goal-conditioned/多目標 RL，讓 LLM 改 w **零重訓**即時變行為；掃 w 可畫 **Pareto 前緣**。
- **PPO 函式庫：CleanRL**（單檔透明、好秀 RL、部署輕）。
- **控制層**：Gemini 結構化輸出 + 驗證安全網 + `ControlLLM` 介面。
- **Sim-to-Real 縮 gap**：系統辨識校準 + Domain Randomization + 現實落差回饋迴圈 + LLM 專屬雜訊（TTFT 抽分布 match 長尾、token 估計帶雜訊、不洩上帝視角）。
- MDP 具體：obs ~15 維、reward = `-(w_lat·正規化TTFT + w_cost·正規化成本) - 逾時懲罰`，正規化用固定基準。

**還沒定（實作時再談）**：控制層「吐權重 vs 吐意圖」、各節點起始數字、本機錨定的 benchmark 來源、前端框架。

---

## 3. 建置順序（照這個做）

1. `backends/` 介面 + `SimulatedBackend`
2. `sim/` Gym 環境跑通（reset/step，隨機策略印 TTFT/成本）
3. `agent/` CleanRL 訓練 + 基準線 → **先拿到「AI 贏基準線」**
4. `server/` + `web/` 左/中面板動起來
5. `control/` 右面板（中文→權重）
6. 校準接真 Ollama/Gemini + Docker/CI/GCP
7. 進階：LSTM 消融、Pareto 圖、gap 圖

---

## 4. 環境 / 鐵則

- **成本節制**：預設全開源、免費（本機 Ollama）；雲端 Gemini 走小額預付，僅用於真打驗證。
- **你家有 RTX 4070 SUPER** → 在家可以真的裝 Ollama、做「邊緣節點真跑」那塊（工作機不一定有 GPU）。
- **隱私**：`.env`(API key) 與工作機的舊交接檔已 gitignore。若 repo 要轉 public，**先掃一遍沒機密**，並考慮移除本檔與 `_交接_給新對話.md`。
- Python 含中文用 `-X utf8`；寫檔用工具、別用 heredoc（踩過雷）。

---

## 5. 教學風格（很重要）

作者 Jacob：**RL 有底子但在鞏固中、LLM 是新學**。要**白話 + 比喻 + 記憶鉤 + 邊做邊教**；**術語一定配真解釋、不硬翻怪詞**（例：別把 high variance 講成「太吵」）。喜歡「先結論再解釋」「一次把觀念串起來」。

---

## 6. 給新對話的第一步

1. 讀 spec（第 0 節連結）。
2. 跟作者確認要不要調整 spec。
3. 走 **superpowers:writing-plans** 產出實作計畫。
4. 從 `backends/` + `sim/` 開工，先讓 `env.reset()/step()` 跑通。

> 註：專案記憶檔在工作機 `C:\Users\Jacob.Chen\.claude\projects\D--RLrepo\memory\`（在家不一定讀得到，故本交接檔自帶重點）。
